<!DOCTYPE html>
<html lang="es">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title><%= title %></title>
  <link rel="stylesheet" href="/css/output.css">
  <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.5.1/css/all.min.css">
</head>
<body class="bg-gray-100 min-h-screen flex items-center justify-center">
  <div class="text-center">
    <div class="mb-8">
      <i class="fas fa-exclamation-triangle text-8xl text-yellow-500"></i>
    </div>
    <h1 class="text-4xl font-bold text-gray-800 mb-4">
      <%= error.status || 500 %>
    </h1>
    <p class="text-xl text-gray-600 mb-8"><%= message %></p>
    <a href="/" class="bg-primary-600 text-white px-6 py-3 rounded-lg hover:bg-primary-700 transition">
      <i class="fas fa-home mr-2"></i> Volver al inicio
    </a>
    <% if (typeof error.stack !== 'undefined' && error.stack) { %>
      <pre class="mt-8 text-left bg-gray-800 text-white p-4 rounded-lg max-w-2xl mx-auto overflow-auto text-sm">
<%= error.stack %>
      </pre>
    <% } %>
  </div>
</body>
</html>

