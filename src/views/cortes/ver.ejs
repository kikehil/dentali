<%
  let gastosHtml = '';
  const gastosList = typeof gastos !== 'undefined' && gastos ? gastos : [];
  let totalGastos = 0;
  let gastosEfectivo = 0;
  let gastosTarjeta = 0;
  let gastosTransferencia = 0;
  let gastosTarjetaAzteca = 0;
  let gastosTarjetaBbva = 0;
  let gastosTarjetaMp = 0;
  let gastosTransferenciaAzteca = 0;
  let gastosTransferenciaBbva = 0;
  let gastosTransferenciaMp = 0;
  
  if (gastosList.length > 0) {
    const gastosEfectivoList = gastosList.filter(g => g.metodoPago === 'efectivo');
    const gastosTarjetaList = gastosList.filter(g => g.metodoPago === 'tarjeta');
    const gastosTransferenciaList = gastosList.filter(g => g.metodoPago === 'transferencia');
    gastosEfectivo = gastosEfectivoList.reduce((sum, g) => sum + parseFloat(g.monto), 0);
    gastosTarjeta = gastosTarjetaList.reduce((sum, g) => sum + parseFloat(g.monto), 0);
    gastosTransferencia = gastosTransferenciaList.reduce((sum, g) => sum + parseFloat(g.monto), 0);
    totalGastos = gastosList.reduce((sum, g) => sum + parseFloat(g.monto), 0);
    
    gastosTarjetaAzteca = gastosTarjetaList.filter(g => g.banco === 'Azteca').reduce((sum, g) => sum + parseFloat(g.monto), 0);
    gastosTarjetaBbva = gastosTarjetaList.filter(g => g.banco === 'BBVA').reduce((sum, g) => sum + parseFloat(g.monto), 0);
    gastosTarjetaMp = gastosTarjetaList.filter(g => g.banco === 'Mercado Pago').reduce((sum, g) => sum + parseFloat(g.monto), 0);
    
    gastosTransferenciaAzteca = gastosTransferenciaList.filter(g => g.banco === 'Azteca').reduce((sum, g) => sum + parseFloat(g.monto), 0);
    gastosTransferenciaBbva = gastosTransferenciaList.filter(g => g.banco === 'BBVA').reduce((sum, g) => sum + parseFloat(g.monto), 0);
    gastosTransferenciaMp = gastosTransferenciaList.filter(g => g.banco === 'Mercado Pago').reduce((sum, g) => sum + parseFloat(g.monto), 0);
    
    let gastosTableRows = '';
    gastosList.forEach(function(g) {
      gastosTableRows += 
        '<tr class="hover:bg-gray-50 transition-colors">' +
          '<td class="px-4 py-3 text-sm text-gray-900">' + moment(g.createdAt).format('DD/MM/YYYY HH:mm') + '</td>' +
          '<td class="px-4 py-3 text-sm text-gray-900">' + g.motivo + '</td>' +
          '<td class="px-4 py-3 text-sm font-medium text-gray-900 text-right">' + formatCurrency(parseFloat(g.monto)) + '</td>' +
          '<td class="px-4 py-3 text-sm text-gray-500 capitalize">' + g.metodoPago + '</td>' +
          '<td class="px-4 py-3 text-sm text-gray-500">' + (g.banco || '-') + '</td>' +
          '<td class="px-4 py-3 text-sm text-gray-500">' + (g.usuario ? g.usuario.nombre : 'N/A') + '</td>' +
        '</tr>';
    });
    
    let tarjetaDetalle = '';
    if (gastosTarjetaList.length > 0) {
      tarjetaDetalle = 
        '<div class="mt-2 text-xs text-gray-600 space-y-1">' +
          (gastosTarjetaAzteca > 0 ? '<p class="flex items-center"><span class="w-2 h-2 bg-blue-400 rounded-full mr-2"></span>Azteca: ' + formatCurrency(gastosTarjetaAzteca) + '</p>' : '') +
          (gastosTarjetaBbva > 0 ? '<p class="flex items-center"><span class="w-2 h-2 bg-blue-500 rounded-full mr-2"></span>BBVA: ' + formatCurrency(gastosTarjetaBbva) + '</p>' : '') +
          (gastosTarjetaMp > 0 ? '<p class="flex items-center"><span class="w-2 h-2 bg-blue-600 rounded-full mr-2"></span>Mercado Pago: ' + formatCurrency(gastosTarjetaMp) + '</p>' : '') +
        '</div>';
    }
    
    let transferenciaDetalle = '';
    if (gastosTransferenciaList.length > 0) {
      transferenciaDetalle = 
        '<div class="mt-2 text-xs text-gray-600 space-y-1">' +
          (gastosTransferenciaAzteca > 0 ? '<p class="flex items-center"><span class="w-2 h-2 bg-purple-400 rounded-full mr-2"></span>Azteca: ' + formatCurrency(gastosTransferenciaAzteca) + '</p>' : '') +
          (gastosTransferenciaBbva > 0 ? '<p class="flex items-center"><span class="w-2 h-2 bg-purple-500 rounded-full mr-2"></span>BBVA: ' + formatCurrency(gastosTransferenciaBbva) + '</p>' : '') +
          (gastosTransferenciaMp > 0 ? '<p class="flex items-center"><span class="w-2 h-2 bg-purple-600 rounded-full mr-2"></span>Mercado Pago: ' + formatCurrency(gastosTransferenciaMp) + '</p>' : '') +
        '</div>';
    }
    
    gastosHtml = 
      '<div class="bg-white rounded-xl shadow-sm border border-gray-200 p-6 mb-6">' +
        '<div class="flex items-center mb-4">' +
          '<i class="fas fa-receipt text-2xl text-gray-600 mr-3"></i>' +
          '<h3 class="text-xl font-bold text-gray-800">Gastos del Período</h3>' +
        '</div>' +
        '<div class="bg-white rounded-lg border border-gray-200 overflow-hidden mb-4">' +
          '<table class="w-full">' +
            '<thead class="bg-gradient-to-r from-gray-50 to-gray-100">' +
              '<tr>' +
                '<th class="px-4 py-3 text-left text-xs font-semibold text-gray-700 uppercase tracking-wider">Fecha</th>' +
                '<th class="px-4 py-3 text-left text-xs font-semibold text-gray-700 uppercase tracking-wider">Motivo</th>' +
                '<th class="px-4 py-3 text-right text-xs font-semibold text-gray-700 uppercase tracking-wider">Monto</th>' +
                '<th class="px-4 py-3 text-left text-xs font-semibold text-gray-700 uppercase tracking-wider">Método</th>' +
                '<th class="px-4 py-3 text-left text-xs font-semibold text-gray-700 uppercase tracking-wider">Banco</th>' +
                '<th class="px-4 py-3 text-left text-xs font-semibold text-gray-700 uppercase tracking-wider">Registrado por</th>' +
              '</tr>' +
            '</thead>' +
            '<tbody class="divide-y divide-gray-200">' +
              gastosTableRows +
            '</tbody>' +
          '</table>' +
        '</div>' +
        '<div class="grid md:grid-cols-3 gap-4 mb-4">' +
          '<div class="bg-red-50 rounded-lg p-4 border-l-4 border-red-500">' +
            '<p class="text-sm text-gray-600 mb-1">Total Efectivo</p>' +
            '<p class="text-xl font-bold text-red-600">' + formatCurrency(gastosEfectivo) + '</p>' +
          '</div>' +
          '<div class="bg-orange-50 rounded-lg p-4 border-l-4 border-orange-500">' +
            '<p class="text-sm text-gray-600 mb-1">Total Tarjeta</p>' +
            '<p class="text-xl font-bold text-orange-600">' + formatCurrency(gastosTarjeta) + '</p>' +
            tarjetaDetalle +
          '</div>' +
          '<div class="bg-pink-50 rounded-lg p-4 border-l-4 border-pink-500">' +
            '<p class="text-sm text-gray-600 mb-1">Total Transferencia</p>' +
            '<p class="text-xl font-bold text-pink-600">' + formatCurrency(gastosTransferencia) + '</p>' +
            transferenciaDetalle +
          '</div>' +
        '</div>' +
        '<div class="bg-gradient-to-r from-gray-50 to-gray-100 rounded-lg p-4 border border-gray-200">' +
          '<p class="text-sm text-gray-600 mb-1">Total Gastos</p>' +
          '<p class="text-3xl font-bold text-gray-800">' + formatCurrency(totalGastos) + '</p>' +
        '</div>' +
      '</div>';
  }
  
  // Detectar si es un corte de bancos (no tiene ventas en efectivo pero sí en tarjeta o transferencia)
  const esCorteBancos = parseFloat(corte.ventasEfectivo || 0) === 0 && 
                       (parseFloat(corte.ventasTarjeta || 0) > 0 || parseFloat(corte.ventasTransferencia || 0) > 0);
  
  // Calcular saldo esperado
  const saldoInicial = parseFloat(corte.saldoInicial || 0);
  const totalVentas = parseFloat(corte.totalVentas || 0);
  const saldoEsperado = saldoInicial + totalVentas - totalGastos;
  
  // Calcular totales de saldos finales
  const saldoFinalEfectivo = parseFloat(corte.saldoFinalEfectivo || 0);
  const saldoFinalTarjeta = parseFloat(corte.saldoFinalTarjetaAzteca || 0) + parseFloat(corte.saldoFinalTarjetaBbva || 0) + parseFloat(corte.saldoFinalTarjetaMp || 0);
  const saldoFinalTransferencia = parseFloat(corte.saldoFinalTransferencia || 0);
  // Para corte de bancos, el total solo incluye tarjeta + transferencia
  const saldoFinalTotal = esCorteBancos ? (saldoFinalTarjeta + saldoFinalTransferencia) : parseFloat(corte.saldoFinal || 0);
  
  // Calcular diferencia
  const diferencia = parseFloat(corte.diferencia || 0);
  const diferenciaEsNegativa = diferencia < 0;
%>
<%- include('../layout', { body: `
<div class="min-h-screen bg-gradient-to-br from-gray-50 to-gray-100 pb-8">
  <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
    
    <!-- ENCABEZADO STICKY -->
    <div class="bg-white rounded-xl shadow-lg border border-gray-200 p-6 mb-6 sticky top-4 z-10">
      <div class="flex flex-col md:flex-row md:items-center md:justify-between">
        <div class="mb-4 md:mb-0">
          <h1 class="text-3xl font-bold text-gray-900 mb-2 flex items-center">
            <i class="fas fa-tooth text-blue-600 mr-3"></i>
            Corte de Caja – Clínica Dental
          </h1>
          <div class="grid grid-cols-2 md:grid-cols-4 gap-4 mt-4">
            <div class="flex items-center text-sm text-gray-600">
              <i class="fas fa-file-alt mr-2 text-gray-400"></i>
              <span class="font-semibold">Folio:</span>
              <span class="ml-2 text-gray-900">#${corte.id}</span>
            </div>
            <div class="flex items-center text-sm text-gray-600">
              <i class="fas fa-calendar mr-2 text-gray-400"></i>
              <span class="font-semibold">Fecha:</span>
              <span class="ml-2 text-gray-900">${moment(corte.fecha).format('DD/MM/YYYY')}</span>
            </div>
            <div class="flex items-center text-sm text-gray-600">
              <i class="fas fa-clock mr-2 text-gray-400"></i>
              <span class="font-semibold">Hora:</span>
              <span class="ml-2 text-gray-900">${corte.hora || 'Saldo Inicial'}</span>
            </div>
            <div class="flex items-center text-sm text-gray-600">
              <i class="fas fa-user mr-2 text-gray-400"></i>
              <span class="font-semibold">Por:</span>
              <span class="ml-2 text-gray-900">${corte.usuario ? corte.usuario.nombre : 'N/A'}</span>
            </div>
          </div>
        </div>
        <div class="flex gap-2">
          <a href="/cortes/historial" class="bg-gray-100 hover:bg-gray-200 text-gray-700 px-4 py-2 rounded-lg transition-colors flex items-center">
            <i class="fas fa-arrow-left mr-2"></i>Volver
          </a>
          <a href="/cortes/${corte.id}/reporte" target="_blank" class="bg-red-500 hover:bg-red-600 text-white px-4 py-2 rounded-lg transition-colors flex items-center shadow-md">
            <i class="fas fa-file-pdf mr-2"></i>Descargar PDF
          </a>
        </div>
      </div>
    </div>

    <!-- KPI CARDS - RESUMEN RÁPIDO -->
    <div class="grid grid-cols-1 md:grid-cols-2 ${esCorteBancos ? 'lg:grid-cols-2' : 'lg:grid-cols-4'} gap-6 mb-6">
      ${!esCorteBancos ? `
      <!-- Saldo Inicial -->
      <div class="bg-gradient-to-br from-blue-50 to-blue-100 rounded-xl shadow-md border border-blue-200 p-6 hover:shadow-lg transition-shadow">
        <div class="flex items-center justify-between mb-3">
          <div class="bg-blue-500 rounded-lg p-3">
            <i class="fas fa-wallet text-white text-xl"></i>
          </div>
          <span class="text-xs font-semibold text-blue-700 bg-blue-200 px-2 py-1 rounded-full">INICIAL</span>
        </div>
        <p class="text-sm text-blue-700 font-medium mb-1">Saldo Inicial</p>
        <p class="text-3xl font-bold text-blue-900">${formatCurrency(saldoInicial)}</p>
        <p class="text-xs text-blue-600 mt-2">Dinero disponible al inicio</p>
      </div>
      ` : ''}

      <!-- Total Ventas -->
      <div class="bg-gradient-to-br from-green-50 to-green-100 rounded-xl shadow-md border border-green-200 p-6 hover:shadow-lg transition-shadow">
        <div class="flex items-center justify-between mb-3">
          <div class="bg-green-500 rounded-lg p-3">
            <i class="fas fa-arrow-up text-white text-xl"></i>
          </div>
          <span class="text-xs font-semibold text-green-700 bg-green-200 px-2 py-1 rounded-full">ENTRADA</span>
        </div>
        <p class="text-sm text-green-700 font-medium mb-1">Total Ventas</p>
        <p class="text-3xl font-bold text-green-900">${formatCurrency(totalVentas)}</p>
        <p class="text-xs text-green-600 mt-2">Ingresos del período</p>
      </div>

      ${!esCorteBancos ? `
      <!-- Total Gastos -->
      <div class="bg-gradient-to-br from-red-50 to-red-100 rounded-xl shadow-md border border-red-200 p-6 hover:shadow-lg transition-shadow">
        <div class="flex items-center justify-between mb-3">
          <div class="bg-red-500 rounded-lg p-3">
            <i class="fas fa-arrow-down text-white text-xl"></i>
          </div>
          <span class="text-xs font-semibold text-red-700 bg-red-200 px-2 py-1 rounded-full">SALIDA</span>
        </div>
        <p class="text-sm text-red-700 font-medium mb-1">Total Gastos</p>
        <p class="text-3xl font-bold text-red-900">${formatCurrency(totalGastos)}</p>
        <p class="text-xs text-red-600 mt-2">Egresos del período</p>
      </div>

      <!-- Saldo Esperado -->
      <div class="bg-gradient-to-br from-purple-50 to-purple-100 rounded-xl shadow-md border border-purple-200 p-6 hover:shadow-lg transition-shadow">
        <div class="flex items-center justify-between mb-3">
          <div class="bg-purple-500 rounded-lg p-3">
            <i class="fas fa-calculator text-white text-xl"></i>
          </div>
          <span class="text-xs font-semibold text-purple-700 bg-purple-200 px-2 py-1 rounded-full">ESPERADO</span>
        </div>
        <p class="text-sm text-purple-700 font-medium mb-1">Saldo Esperado</p>
        <p class="text-3xl font-bold text-purple-900">${formatCurrency(saldoEsperado)}</p>
        <p class="text-xs text-purple-600 mt-2">Inicial + Ventas - Gastos</p>
      </div>
      ` : ''}
    </div>

    <!-- VENTAS DESGLOSADAS POR MÉTODO -->
    <div class="bg-white rounded-xl shadow-sm border border-gray-200 p-6 mb-6">
      <div class="flex items-center mb-6">
        <i class="fas fa-chart-line text-2xl text-gray-600 mr-3"></i>
        <h2 class="text-2xl font-bold text-gray-800">Ventas del Período</h2>
      </div>
      <div class="grid md:grid-cols-${esCorteBancos ? '2' : '3'} gap-6">
        ${!esCorteBancos ? `
        <!-- Efectivo -->
        <div class="bg-gradient-to-br from-green-50 to-green-100 rounded-lg p-5 border-l-4 border-green-500 shadow-sm">
          <div class="flex items-center justify-between mb-3">
            <div class="flex items-center">
              <i class="fas fa-money-bill-wave text-green-600 text-xl mr-2"></i>
              <h3 class="text-lg font-semibold text-green-800">Efectivo</h3>
            </div>
          </div>
          <p class="text-3xl font-bold text-green-900 mb-2">${formatCurrency(parseFloat(corte.ventasEfectivo || 0))}</p>
          <div class="mt-3 pt-3 border-t border-green-200">
            <div class="w-full bg-green-200 rounded-full h-2">
              <div class="bg-green-500 h-2 rounded-full" style="width: ${totalVentas > 0 ? (parseFloat(corte.ventasEfectivo || 0) / totalVentas * 100) : 0}%"></div>
            </div>
            <p class="text-xs text-green-700 mt-1">${totalVentas > 0 ? ((parseFloat(corte.ventasEfectivo || 0) / totalVentas * 100).toFixed(1)) : 0}% del total</p>
          </div>
        </div>
        ` : ''}

        <!-- Tarjeta -->
        <div class="bg-gradient-to-br from-blue-50 to-blue-100 rounded-lg p-5 border-l-4 border-blue-500 shadow-sm">
          <div class="flex items-center justify-between mb-3">
            <div class="flex items-center">
              <i class="fas fa-credit-card text-blue-600 text-xl mr-2"></i>
              <h3 class="text-lg font-semibold text-blue-800">Tarjeta</h3>
            </div>
          </div>
          <p class="text-3xl font-bold text-blue-900 mb-2">${formatCurrency(parseFloat(corte.ventasTarjeta || 0))}</p>
          ${parseFloat(corte.ventasTarjeta || 0) > 0 ? 
            '<div class="mt-3 space-y-2 text-xs">' +
              '<p class="text-blue-700 font-medium">Desglose por banco:</p>' +
              '<div class="space-y-1">' +
                (parseFloat(corte.ventasTarjetaAzteca || 0) > 0 ? '<p class="flex items-center text-blue-600"><span class="w-2 h-2 bg-blue-400 rounded-full mr-2"></span>Azteca: ' + formatCurrency(parseFloat(corte.ventasTarjetaAzteca || 0)) + '</p>' : '') +
                (parseFloat(corte.ventasTarjetaBbva || 0) > 0 ? '<p class="flex items-center text-blue-600"><span class="w-2 h-2 bg-blue-500 rounded-full mr-2"></span>BBVA: ' + formatCurrency(parseFloat(corte.ventasTarjetaBbva || 0)) + '</p>' : '') +
                (parseFloat(corte.ventasTarjetaMp || 0) > 0 ? '<p class="flex items-center text-blue-600"><span class="w-2 h-2 bg-blue-600 rounded-full mr-2"></span>Mercado Pago: ' + formatCurrency(parseFloat(corte.ventasTarjetaMp || 0)) + '</p>' : '') +
              '</div>' +
            '</div>' : ''
          }
          <div class="mt-3 pt-3 border-t border-blue-200">
            <div class="w-full bg-blue-200 rounded-full h-2">
              <div class="bg-blue-500 h-2 rounded-full" style="width: ${totalVentas > 0 ? (parseFloat(corte.ventasTarjeta || 0) / totalVentas * 100) : 0}%"></div>
            </div>
            <p class="text-xs text-blue-700 mt-1">${totalVentas > 0 ? ((parseFloat(corte.ventasTarjeta || 0) / totalVentas * 100).toFixed(1)) : 0}% del total</p>
          </div>
        </div>

        <!-- Transferencia -->
        <div class="bg-gradient-to-br from-purple-50 to-purple-100 rounded-lg p-5 border-l-4 border-purple-500 shadow-sm">
          <div class="flex items-center justify-between mb-3">
            <div class="flex items-center">
              <i class="fas fa-exchange-alt text-purple-600 text-xl mr-2"></i>
              <h3 class="text-lg font-semibold text-purple-800">Transferencia</h3>
            </div>
          </div>
          <p class="text-3xl font-bold text-purple-900 mb-2">${formatCurrency(parseFloat(corte.ventasTransferencia || 0))}</p>
          <div class="mt-3 pt-3 border-t border-purple-200">
            <div class="w-full bg-purple-200 rounded-full h-2">
              <div class="bg-purple-500 h-2 rounded-full" style="width: ${totalVentas > 0 ? (parseFloat(corte.ventasTransferencia || 0) / totalVentas * 100) : 0}%"></div>
            </div>
            <p class="text-xs text-purple-700 mt-1">${totalVentas > 0 ? ((parseFloat(corte.ventasTransferencia || 0) / totalVentas * 100).toFixed(1)) : 0}% del total</p>
          </div>
        </div>
      </div>
      <div class="mt-6 bg-gradient-to-r from-gray-50 to-gray-100 rounded-lg p-4 border border-gray-200">
        <div class="flex items-center justify-between">
          <p class="text-sm font-semibold text-gray-700">Total Ventas del Período</p>
          <p class="text-3xl font-bold text-gray-900">${formatCurrency(totalVentas)}</p>
        </div>
      </div>
    </div>

    <!-- GASTOS DEL PERÍODO -->
    ${!esCorteBancos ? gastosHtml : ''}

    <!-- SALDOS FINALES -->
    <div class="bg-white rounded-xl shadow-sm border border-gray-200 p-6 mb-6">
      <div class="flex items-center mb-6">
        <i class="fas fa-coins text-2xl text-gray-600 mr-3"></i>
        <h2 class="text-2xl font-bold text-gray-800">Saldos Finales Registrados</h2>
      </div>
      <div class="grid md:grid-cols-${esCorteBancos ? '2' : '3'} gap-6 mb-6">
        ${!esCorteBancos ? `
        <div class="bg-gradient-to-br from-green-50 to-green-100 rounded-lg p-5 border-l-4 border-green-500 shadow-sm">
          <div class="flex items-center mb-3">
            <i class="fas fa-money-bill-wave text-green-600 text-xl mr-2"></i>
            <h3 class="text-lg font-semibold text-green-800">Efectivo Final</h3>
          </div>
          <p class="text-3xl font-bold text-green-900">${formatCurrency(saldoFinalEfectivo)}</p>
        </div>
        ` : ''}
        <div class="bg-gradient-to-br from-blue-50 to-blue-100 rounded-lg p-5 border-l-4 border-blue-500 shadow-sm">
          <div class="flex items-center mb-3">
            <i class="fas fa-credit-card text-blue-600 text-xl mr-2"></i>
            <h3 class="text-lg font-semibold text-blue-800">Tarjeta Final</h3>
          </div>
          <p class="text-3xl font-bold text-blue-900">${formatCurrency(saldoFinalTarjeta)}</p>
          <div class="mt-3 text-xs text-blue-700 space-y-1">
            <p>Azteca: ${formatCurrency(parseFloat(corte.saldoFinalTarjetaAzteca || 0))}</p>
            <p>BBVA: ${formatCurrency(parseFloat(corte.saldoFinalTarjetaBbva || 0))}</p>
            <p>Mercado Pago: ${formatCurrency(parseFloat(corte.saldoFinalTarjetaMp || 0))}</p>
          </div>
        </div>
        <div class="bg-gradient-to-br from-purple-50 to-purple-100 rounded-lg p-5 border-l-4 border-purple-500 shadow-sm">
          <div class="flex items-center mb-3">
            <i class="fas fa-exchange-alt text-purple-600 text-xl mr-2"></i>
            <h3 class="text-lg font-semibold text-purple-800">Transferencia Final</h3>
          </div>
          <p class="text-3xl font-bold text-purple-900">${formatCurrency(saldoFinalTransferencia)}</p>
        </div>
      </div>
      <div class="bg-gradient-to-r from-gray-50 to-gray-100 rounded-lg p-5 border border-gray-200">
        <div class="flex items-center justify-between">
          <div>
            <p class="text-sm font-semibold text-gray-700 mb-1">Total Saldo Final Calculado</p>
            <p class="text-xs text-gray-600">${esCorteBancos ? 'Suma de tarjeta + transferencia' : 'Suma de efectivo + tarjeta + transferencia'}</p>
          </div>
          <p class="text-3xl font-bold text-gray-900">${formatCurrency(saldoFinalTotal)}</p>
        </div>
      </div>
    </div>

    ${!esCorteBancos ? `
    <!-- BLOQUE DE DIFERENCIA - MÁS VISIBLE -->
    <div class="bg-white rounded-xl shadow-lg border-2 ${diferenciaEsNegativa ? 'border-red-300' : 'border-green-300'} p-8 mb-6">
      <div class="text-center">
        <div class="inline-flex items-center justify-center w-20 h-20 rounded-full ${diferenciaEsNegativa ? 'bg-red-100' : 'bg-green-100'} mb-4">
          <i class="fas ${diferenciaEsNegativa ? 'fa-exclamation-triangle' : 'fa-check-circle'} text-4xl ${diferenciaEsNegativa ? 'text-red-600' : 'text-green-600'}"></i>
        </div>
        <h2 class="text-2xl font-bold ${diferenciaEsNegativa ? 'text-red-800' : 'text-green-800'} mb-2">
          DIFERENCIA EN CORTE
        </h2>
        <p class="text-5xl font-bold ${diferenciaEsNegativa ? 'text-red-600' : 'text-green-600'} mb-4">
          ${diferenciaEsNegativa ? '-' : '+'}${formatCurrency(Math.abs(diferencia))}
        </p>
        <div class="inline-flex items-center px-4 py-2 rounded-full ${diferenciaEsNegativa ? 'bg-red-100 text-red-800' : 'bg-green-100 text-green-800'} font-semibold mb-4">
          <i class="fas ${diferenciaEsNegativa ? 'fa-times-circle' : 'fa-check-circle'} mr-2"></i>
          ${diferenciaEsNegativa ? 'Faltante Detectado' : 'Cuadre Correcto'}
        </div>
        <p class="text-sm text-gray-600 max-w-2xl mx-auto">
          La diferencia se genera cuando el total esperado (${formatCurrency(saldoEsperado)}) no coincide con el total registrado (${formatCurrency(saldoFinalTotal)}).
          ${diferenciaEsNegativa ? 'Se detectó un faltante de dinero.' : 'El corte cuadra correctamente.'}
        </p>
        <div class="mt-6 grid grid-cols-2 gap-4 max-w-md mx-auto">
          <div class="bg-gray-50 rounded-lg p-4">
            <p class="text-xs text-gray-600 mb-1">Saldo Esperado</p>
            <p class="text-lg font-bold text-gray-900">${formatCurrency(saldoEsperado)}</p>
          </div>
          <div class="bg-gray-50 rounded-lg p-4">
            <p class="text-xs text-gray-600 mb-1">Saldo Registrado</p>
            <p class="text-lg font-bold text-gray-900">${formatCurrency(saldoFinalTotal)}</p>
          </div>
        </div>
      </div>
    </div>
    ` : ''}

    <!-- SECCIÓN COLAPSABLE: ¿Cómo se calcula este corte? -->
    <div class="bg-white rounded-xl shadow-sm border border-gray-200 p-6 mb-6">
      <button onclick="toggleExplicacion()" class="w-full flex items-center justify-between text-left focus:outline-none">
        <div class="flex items-center">
          <i class="fas fa-info-circle text-blue-600 text-xl mr-3"></i>
          <h3 class="text-lg font-bold text-gray-800">¿Cómo se calcula este corte?</h3>
        </div>
        <i id="iconExplicacion" class="fas fa-chevron-down text-gray-400 transition-transform"></i>
      </button>
      <div id="explicacionCorte" class="hidden mt-4 pt-4 border-t border-gray-200">
        <div class="bg-gradient-to-r from-blue-50 to-indigo-50 rounded-lg p-6">
          <h4 class="font-semibold text-gray-800 mb-4">Fórmula del Corte de Caja:</h4>
          <div class="space-y-3">
            <div class="flex items-center">
              <div class="bg-blue-100 rounded-lg p-3 mr-4">
                <i class="fas fa-wallet text-blue-600"></i>
              </div>
              <div>
                <p class="text-sm font-semibold text-gray-700">Saldo Inicial</p>
                <p class="text-lg font-bold text-blue-900">${formatCurrency(saldoInicial)}</p>
                <p class="text-xs text-gray-600">Dinero disponible al inicio del período</p>
              </div>
            </div>
            <div class="flex items-center text-2xl text-gray-400">
              <i class="fas fa-plus mx-4"></i>
            </div>
            <div class="flex items-center">
              <div class="bg-green-100 rounded-lg p-3 mr-4">
                <i class="fas fa-arrow-up text-green-600"></i>
              </div>
              <div>
                <p class="text-sm font-semibold text-gray-700">Total Ventas</p>
                <p class="text-lg font-bold text-green-900">${formatCurrency(totalVentas)}</p>
                <p class="text-xs text-gray-600">Ingresos por ventas del período</p>
              </div>
            </div>
            <div class="flex items-center text-2xl text-gray-400">
              <i class="fas fa-minus mx-4"></i>
            </div>
            <div class="flex items-center">
              <div class="bg-red-100 rounded-lg p-3 mr-4">
                <i class="fas fa-arrow-down text-red-600"></i>
              </div>
              <div>
                <p class="text-sm font-semibold text-gray-700">Total Gastos</p>
                <p class="text-lg font-bold text-red-900">${formatCurrency(totalGastos)}</p>
                <p class="text-xs text-gray-600">Egresos del período</p>
              </div>
            </div>
            <div class="flex items-center text-2xl text-gray-400">
              <i class="fas fa-equals mx-4"></i>
            </div>
            <div class="flex items-center">
              <div class="bg-purple-100 rounded-lg p-3 mr-4">
                <i class="fas fa-calculator text-purple-600"></i>
              </div>
              <div>
                <p class="text-sm font-semibold text-gray-700">Saldo Esperado</p>
                <p class="text-lg font-bold text-purple-900">${formatCurrency(saldoEsperado)}</p>
                <p class="text-xs text-gray-600">Resultado esperado del cálculo</p>
              </div>
            </div>
          </div>
          <div class="mt-6 pt-4 border-t border-gray-200">
            <p class="text-sm text-gray-700">
              <strong>Diferencia:</strong> Se calcula restando el Saldo Final Registrado del Saldo Esperado.
              Si la diferencia es negativa, hay un faltante. Si es positiva o cero, el corte cuadra correctamente.
            </p>
          </div>
        </div>
      </div>
    </div>

    <!-- OBSERVACIONES -->
    ${corte.observaciones ? 
      '<div class="bg-white rounded-xl shadow-sm border border-gray-200 p-6">' +
        '<div class="flex items-center mb-4">' +
          '<i class="fas fa-sticky-note text-2xl text-gray-600 mr-3"></i>' +
          '<h3 class="text-xl font-bold text-gray-800">Observaciones</h3>' +
        '</div>' +
        '<div class="bg-gray-50 rounded-lg p-4 border-l-4 border-gray-400">' +
          '<p class="text-gray-700 whitespace-pre-wrap">' + corte.observaciones + '</p>' +
        '</div>' +
      '</div>' : ''
    }
  </div>
</div>

<script>
function toggleExplicacion() {
  const explicacion = document.getElementById('explicacionCorte');
  const icon = document.getElementById('iconExplicacion');
  explicacion.classList.toggle('hidden');
  icon.classList.toggle('rotate-180');
}
</script>
` }) %>
